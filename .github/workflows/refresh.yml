# Trilogy Renewals Dashboard — Salesforce Data Refresh
# Triggers: scheduled (Mon-Fri 7AM UTC), manual, or API dispatch
#
# Required secrets:
#   SF_USERNAME         - Salesforce username (email)
#   SF_PASSWORD         - Salesforce password
#   SF_SECURITY_TOKEN   - Salesforce security token
#   SF_CLIENT_ID        - Connected App consumer key (required by SF for password OAuth flow)
#   SF_CLIENT_SECRET    - Connected App consumer secret (required by SF for password OAuth flow)
#   SUPABASE_URL        - Supabase project URL
#   SUPABASE_SERVICE_KEY - Supabase service_role key
#
# Optional secrets:
#   SF_LOGIN_URL        - Login URL (default: https://login.salesforce.com)
#                         Use https://test.salesforce.com for sandboxes

name: Refresh Salesforce Data

on:
  schedule:
    # Mon-Fri at 7:00 AM UTC (3:00 AM ET)
    - cron: "0 7 * * 1-5"

  workflow_dispatch:
    inputs:
      skip_activities:
        description: "Skip activities query"
        required: false
        default: "false"
        type: boolean

env:
  SF_USERNAME: ${{ secrets.SF_USERNAME }}
  SF_PASSWORD: ${{ secrets.SF_PASSWORD }}
  SF_SECURITY_TOKEN: ${{ secrets.SF_SECURITY_TOKEN }}
  SF_CLIENT_ID: ${{ secrets.SF_CLIENT_ID }}
  SF_CLIENT_SECRET: ${{ secrets.SF_CLIENT_SECRET }}
  SF_LOGIN_URL: ${{ secrets.SF_LOGIN_URL }}
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Query Salesforce — All Opportunities
        run: |
          echo "Querying all opportunities..."
          python3 lib/query_sf_direct.py data/sf_latest.json

      - name: Split into per-gate files
        run: |
          echo "Splitting sf_latest.json into gate files..."
          python3 lib/split_gates.py

      - name: Query Salesforce — Activities
        if: ${{ github.event.inputs.skip_activities != 'true' }}
        run: |
          echo "Querying Activities..."
          python3 lib/query_sf_direct.py data/sf_activities_latest.json --soql-key activities_soql --allow-empty

      - name: Evaluate Gates
        run: |
          echo "Running 7-gate evaluation framework..."
          python3 lib/evaluate_gates.py

      - name: Agent Evaluation
        if: ${{ env.ANTHROPIC_API_KEY != '' }}
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Running AI reasoning layer..."
          python3 lib/agent_evaluate.py || echo "Agent evaluation failed — Phase 1 output remains as fallback"

      - name: Write to Supabase
        run: |
          echo "Upserting data to Supabase..."
          python3 lib/write_to_supabase.py

      - name: Summary
        run: |
          echo "## Refresh Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| File | Records |" >> $GITHUB_STEP_SUMMARY
          echo "|------|---------|" >> $GITHUB_STEP_SUMMARY
          for f in data/sf_*.json; do
            if [[ -f "$f" ]]; then
              count=$(python3 -c "import json; print(len(json.load(open('$f'))))" 2>/dev/null || echo "0")
              echo "| $(basename $f) | $count |" >> $GITHUB_STEP_SUMMARY
            fi
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          if [[ -f "data/gate_evaluations.json" ]]; then
            echo "### Gate Evaluation" >> $GITHUB_STEP_SUMMARY
            eval_count=$(python3 -c "import json;print(json.load(open('data/gate_evaluations.json'))['summary']['total_opportunities'])" 2>/dev/null || echo "0")
            violations=$(python3 -c "import json;print(json.load(open('data/gate_evaluations.json'))['summary']['total_violations'])" 2>/dev/null || echo "0")
            arr_risk=$(python3 -c "import json;print(int(json.load(open('data/gate_evaluations.json'))['summary']['total_arr_at_risk']))" 2>/dev/null || echo "0")
            risk_crit=$(python3 -c "import json;print(json.load(open('data/gate_evaluations.json'))['summary']['by_risk_level']['critical'])" 2>/dev/null || echo "0")
            risk_high=$(python3 -c "import json;print(json.load(open('data/gate_evaluations.json'))['summary']['by_risk_level']['high'])" 2>/dev/null || echo "0")
            risk_med=$(python3 -c "import json;print(json.load(open('data/gate_evaluations.json'))['summary']['by_risk_level']['medium'])" 2>/dev/null || echo "0")
            risk_low=$(python3 -c "import json;print(json.load(open('data/gate_evaluations.json'))['summary']['by_risk_level']['low'])" 2>/dev/null || echo "0")
            echo "- **Evaluated:** $eval_count opportunities" >> $GITHUB_STEP_SUMMARY
            echo "- **Violations:** $violations" >> $GITHUB_STEP_SUMMARY
            echo "- **ARR at risk:** $arr_risk" >> $GITHUB_STEP_SUMMARY
            echo "- **Risk:** $risk_crit critical, $risk_high high, $risk_med medium, $risk_low low" >> $GITHUB_STEP_SUMMARY
          fi
